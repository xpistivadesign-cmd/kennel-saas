"use server";

type IndividualId = string;

// ide jön a pure calculation logic
// NINCS import "server-only"
// NINCS client import
// NINCS React kapcsolat

export async function calculateCOI() {
  return 0; // placeholder
}
